{
  "project": "citysocial",
  "product_context": "CitySocial is a modular-monolith social platform for city residents (currently Austin), built as one Rails app whose features live as near-independent modules (mountable engines) under components/. platform_core owns identity (User), the Follow social graph, a public Graph API, and an event bus; feed is the reference module (posts + timeline); communities is subreddit-style (join, post, comment, up/down vote); marketplace is classified listings with photos; restaurants is head-to-head Elo ranking of local spots. Users are residents who follow each other and engage through these modules. 'Better' means tightening the existing engagement loops (do -> others see it -> they get notified/react), filling in missing empty/loading/error and accessibility states on views that already ship, and polishing the UX of already-shipped modules. It does NOT mean speculative net-new features nobody asked for. Every view must compose the PlatformCore::Ui components and Tailwind v4 design tokens (no hand-rolled markup, no hardcoded hex). Modules may depend only on platform_core and must cross boundaries only via events or a sibling's app/public API (Packwerk enforces this).",
  "out_of_scope": [
    "Anything requiring a schema migration",
    "Anything touching auth, authorization, sessions, or payments",
    "New third-party dependencies (gems or JS)",
    "Standing up a whole new module (that is the human-run /new-app-module flow)",
    "Cross-module references that violate the Packwerk boundary contract",
    "Editing the @theme design tokens or the standalone Tailwind build config"
  ],
  "check_command": "bin/verify",
  "next_id": 5,
  "items": [
    {
      "id": "F-002",
      "title": "Add empty state to the restaurants leaderboard",
      "problem": "The leaderboard renders a bare header-only table when there are no restaurants, unlike every other index view which shows an empty state.",
      "evidence": "components/restaurants/app/views/restaurants/leaderboard/index.html.erb:9 — TableComponent iterates @restaurants with no any? guard; Restaurants::Restaurant.ranked (restaurant.rb:5) is an unfiltered order, so a fresh install shows a bare table.",
      "acceptance": [
        "With zero restaurants, /restaurants/leaderboard renders a PlatformCore::Ui::EmptyState instead of an empty table",
        "The empty state composes shared UI components and design tokens with a CTA back to matchups",
        "When restaurants exist, the ranked table renders exactly as before",
        "Covered by a request spec asserting the empty-state copy renders with zero restaurants"
      ],
      "impact": 2,
      "effort": 1,
      "confidence": 0.9,
      "score": 1.8,
      "status": "done",
      "origin": "auto",
      "attempts": 1,
      "created": "2026-08-04",
      "pr": "https://github.com/danecjensen/citysocial/pull/1"
    },
    {
      "id": "F-001",
      "title": "Add a post composer to the Feed index",
      "problem": "The feed (the app's root page) shows posts but gives a logged-in user no UI to write one; the create action and route exist but nothing renders a form.",
      "evidence": "components/feed/app/views/feed/posts/index.html.erb has no form/new-post button; feed/posts_controller.rb:9 defines create and config/routes.rb exposes POST /feed/posts, but no view links to it — a dead-end engagement loop on root ('/' redirects to /feed).",
      "acceptance": [
        "Logged-in users see a composer (a PlatformCore::Ui Card with a body text_area + submit) at the top of the feed",
        "Submitting a non-empty body creates a Feed::Post and shows it in the feed",
        "Submitting a blank body is rejected gracefully with a flash, not a 500 (controller uses create! today)",
        "Covered by a feed request spec (none exists yet)"
      ],
      "impact": 4,
      "effort": 2,
      "confidence": 0.85,
      "score": 1.7,
      "status": "ready",
      "origin": "auto",
      "attempts": 0,
      "created": "2026-08-04",
      "pr": null
    },
    {
      "id": "F-003",
      "title": "Give community vote buttons accessible names",
      "problem": "Up/down vote buttons render only the glyphs '▲'/'▼' with no accessible name, so screen readers cannot announce their purpose.",
      "evidence": "components/communities/app/views/communities/posts/_vote.html.erb:4,8 — ButtonComponent content is a bare glyph; button_component.rb:24 has no aria-label/label param, so the component must be extended to pass an accessible name through.",
      "acceptance": [
        "Upvote/downvote controls expose an accessible name (e.g. aria-label 'Upvote' / 'Downvote')",
        "PlatformCore::Ui::ButtonComponent supports passing an accessible label without breaking existing call sites",
        "Design catalog (/design) still renders and the button variants are unchanged visually",
        "Covered by a component or request spec asserting the accessible name is present"
      ],
      "impact": 3,
      "effort": 2,
      "confidence": 0.7,
      "score": 1.05,
      "status": "ready",
      "origin": "auto",
      "attempts": 0,
      "created": "2026-08-04",
      "pr": null
    },
    {
      "id": "F-004",
      "title": "Remove N+1 author lookup in the feed timeline",
      "problem": "Rendering the timeline issues one User query per post because each post resolves its author individually.",
      "evidence": "components/feed/app/models/feed/post.rb:6-8 — #author calls PlatformCore::Graph.user(author_id) (graph.rb:15-17 does User.find_by per call); feed/posts/index.html.erb:8 calls post.author&.handle inside the @posts loop → N+1. A fix likely needs a batch Graph.users(ids) public API method.",
      "acceptance": [
        "Rendering the feed for N posts issues O(1) user queries, not O(N)",
        "Author handles still render correctly for every post",
        "A new batch accessor (if added) lives in platform_core app/public and does not violate Packwerk",
        "Covered by a spec asserting the query count does not grow with post count"
      ],
      "impact": 3,
      "effort": 2,
      "confidence": 0.65,
      "score": 0.975,
      "status": "ready",
      "origin": "auto",
      "attempts": 0,
      "created": "2026-08-04",
      "pr": null
    }
  ]
}
