module <%= class_name %>
  # All cross-module wiring for <%= class_name %> lives here: what it listens
  # for, and (by convention) what it publishes. Keep this file as the honest
  # description of how this module connects to the rest of the system.
  module Events
    module_function

    def subscribe!
      # Example:
      # PlatformCore::EventBus.subscribe("feed.post_created", method(:on_post_created))
    end

    # Events this module PUBLISHES (documented for discoverability):
    #   - "<%= file_name %>.something_happened"
  end
end
