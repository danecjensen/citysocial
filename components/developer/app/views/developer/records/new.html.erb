<% content_for :title, "Developer · New #{@model.name}" %>

<%= render PlatformCore::Ui::PageHeaderComponent.new(
      title: "New #{@model.name}",
      subtitle: "Create a row in #{@model.table_name}. Model validations and callbacks still run."
    ) %>

<%= render PlatformCore::Ui::CardComponent.new do %>
  <%= render "form" %>
<% end %>
